import React from 'react';
import { Text, View } from 'react-native';

const ListContainer = () => (
    <View style={{ flex: 1 }}>
        <Text>List</Text>
    </View>
);

export default ListContainer;
